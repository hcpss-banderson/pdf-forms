---
interface Props {
  level?: number
}
const { level = 2 } = Astro.props
const Element = `h${level}`
---
<Element>
  <slot></slot>
</Element>

<style>
  h1, h2, h3, h4, h5, h6 {
    font-family: 'Open Sans',sans-serif;
  }
  h2 {
    margin: 2rem 0 1rem 0;
  }
</style>